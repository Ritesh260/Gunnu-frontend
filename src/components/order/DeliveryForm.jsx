import UpiPaymentSection from "./UpiPaymentSection";

export default function DeliveryForm({ customerName, phone, address, landmark, payment, selectedUPI, transactionId, paymentScreenshot, cart, total, handleSubmit, setCustomerName, setPhone, setAddress, setLandmark, setPayment, setSelectedUPI, setTransactionId, setPaymentScreenshot }) {
    const inputStyle = { background: "#0d0d0d", border: "1px solid rgba(255,255,255,.07)", color: "white" };
    return <><h2 className="text-lg font-bold mb-5">Delivery Details</h2><form onSubmit={handleSubmit} className="space-y-3"><div className="grid grid-cols-1 sm:grid-cols-2 gap-3"><input type="text" required value={customerName} onChange={(event) => setCustomerName(event.target.value)} placeholder="Full Name" className="px-4 py-3 text-sm rounded-xl w-full" style={inputStyle} /><input type="tel" required value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="Phone Number" className="px-4 py-3 text-sm rounded-xl w-full" style={inputStyle} /></div><textarea required rows="3" value={address} onChange={(event) => setAddress(event.target.value)} placeholder="Full Delivery Address" className="w-full px-4 py-3 text-sm rounded-xl resize-none" style={inputStyle} /><input type="text" value={landmark} onChange={(event) => setLandmark(event.target.value)} placeholder="Nearby Landmark (optional)" className="w-full px-4 py-3 text-sm rounded-xl" style={inputStyle} /><select value={payment} onChange={(event) => setPayment(event.target.value)} className="w-full px-4 py-3 text-sm rounded-xl" style={inputStyle}><option>Cash On Delivery</option><option>UPI Payment</option></select>{payment === "UPI Payment" && <UpiPaymentSection {...{ selectedUPI, transactionId, setSelectedUPI, setTransactionId, paymentScreenshot, setPaymentScreenshot }} />}<button type="submit" className="add-btn w-full py-4 rounded-xl text-sm font-bold flex items-center justify-center gap-2 mt-2" style={{ background: "linear-gradient(135deg,#991b1b 0%,#d97706 100%)", color: "white" }}>Confirm Order{cart.length > 0 && ` · ₹${total}`}</button></form></>;
}