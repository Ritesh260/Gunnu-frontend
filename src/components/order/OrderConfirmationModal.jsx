import { Check } from "lucide-react";

export default function OrderConfirmationModal({ show, cart, total, payment, getCartItemTotal, onCancel, onConfirm }) {
    if (!show) return null;
    const paymentMessage = payment === "Cash On Delivery" ? "Click OK to open WhatsApp and confirm your order. We'll start preparing right away!" : "Click OK to open WhatsApp and share your payment screenshot for verification.";
    return <div className="fixed inset-0 z-50 flex items-center justify-center px-3 sm:px-4 py-4" style={{ background: "rgba(0,0,0,.88)", backdropFilter: "blur(12px)" }}><div className="w-full max-w-md rounded-2xl p-5 sm:p-8 text-center max-h-[90vh] overflow-y-auto" style={{ background: "#111", border: "1px solid rgba(234,179,8,.15)", animation: "popIn .3s cubic-bezier(.22,1,.36,1) both" }}><div className="w-14 h-14 sm:w-16 sm:h-16 mx-auto rounded-full flex items-center justify-center mb-5" style={{ background: "linear-gradient(135deg,#16a34a,#15803d)" }}><Check className="text-white" size={30} strokeWidth={3} /></div><h2 className="text-3xl sm:text-4xl font-extrabold"><span className="gold-text">Thank You</span></h2><p className="text-yellow-600 text-sm mt-2 font-medium">Your order is ready to confirm</p><div className="mt-5 rounded-xl p-4 text-left space-y-3" style={{ background: "#0d0d0d", border: "1px solid rgba(255,255,255,.06)" }}>{cart.map((item, index) => <div key={`${item.name}-${item.size}-${index}`} className="border-b border-white/5 pb-3 last:border-0 last:pb-0"><div className="flex justify-between gap-3"><div className="min-w-0"><p className="text-sm text-gray-300 font-medium truncate">{item.name}</p><p className="text-[10px] text-gray-600 mt-1">Category: {item.category}</p><p className="text-[10px] text-gray-600 mt-1">{item.size === "half" ? "Half" : "Full"} × {item.qty} @ ₹{item.unitPrice}</p></div><span className="text-white font-medium text-sm flex-shrink-0">₹{getCartItemTotal(item)}</span></div></div>)}<div className="flex justify-between text-sm font-bold pt-2" style={{ borderTop: "1px solid rgba(255,255,255,.06)" }}><span className="text-gray-400">Total</span><span className="text-yellow-500">₹{total}</span></div></div><p className="text-gray-500 text-xs sm:text-sm mt-5 leading-relaxed">{paymentMessage} 🍜</p><div className="modal-buttons flex gap-3 mt-6"><button type="button" onClick={onCancel} className="flex-1 py-3 rounded-xl text-sm" style={{ border: "1px solid rgba(255,255,255,.08)", color: "white" }}>Cancel</button><button type="button" onClick={onConfirm} className="add-btn flex-1 py-3 rounded-xl text-sm font-bold" style={{ background: "linear-gradient(135deg,#991b1b,#d97706)", color: "white" }}>OK, Confirm</button></div></div></div>;
}